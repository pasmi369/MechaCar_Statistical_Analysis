#========================================================================================================================
#==============================================DELIVERABLE 1 ============================================================
#========================================================================================================================
#Adding Libraries
library(dplyr)
#========================================================================================================================
# Import and Read
mecha_data <- read.csv("MechaCar_mpg.csv",stringsAsFactors = F,check.names = F)
#========================================================================================================================
#Perform Linear regression on MPG Data
mecha_lm <- lm(mpg ~ vehicle_length + vehicle_weight + spoiler_angle + ground_clearance + AWD,data=mecha_data)
#========================================================================================================================
#Summarize the mpg Dataset
summary(mecha_lm)
#========================================================================================================================
#==============================================DELIVERABLE 2 ============================================================
#========================================================================================================================
#Create and read suspension_coi.csv
suspension_data <- read.csv("Suspension_Coil.csv",stringsAsFactors = F, check.names = F)
#========================================================================================================================
#Create a summary for all PSI Data
total_summary <- suspension_data %>% 
  summarize(Mean=mean(PSI),Median=median(PSI),Variance=var(PSI),SD=sd(PSI))
#========================================================================================================================
#Create Rscript for lot_summary()using group_by
lot_summary <- suspension_data %>% group_by(Manufacturing_Lot) %>% 
  summarize(Mean=mean(PSI),Median=median(PSI),Variance=var(PSI),SD=sd(PSI), .groups='keep')
#========================================================================================================================
#==============================================DELIVERABLE 3 ============================================================
#========================================================================================================================
# T-tests on Suspension Coils
t.test(suspension_data$PSI,mu = 1500)
#========================================================================================================================
#Create a subset for Lot 1 Cars
t.test(subset(suspension_data,Manufacturing_Lot=="Lot1")$PSI,mu = 1500)
#Create a subset for Lot 2 Cars
t.test(subset(suspension_data,Manufacturing_Lot=="Lot2")$PSI,mu = 1500)
#Create a subset for Lot 3 Cars
t.test(subset(suspension_data,Manufacturing_Lot=="Lot3")$PSI,mu = 1500)
#========================================================================================================================